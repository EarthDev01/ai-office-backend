# CLAUDE.md

กฎของ repo นี้ (หลังบ้าน AI Office — Go + widget) ใช้กับทุกคนที่แก้โค้ด รวมถึง AI ช่วยเขียนโค้ด

## 1. แก้หรือเพิ่มอะไร ต้องเช็กคู่มือทุกครั้ง

- คู่มือผู้ใช้คือ `docs/guide.md` ใน repo นี้ (แหล่งเดียว) — หน้า `/guide` ของคอนโซลดึงไปแสดง และผู้ช่วย AI ในคอนโซลใช้ตอบวิธีใช้ · รูปแบบหัวข้อ `## ชื่อ {#id}` ดูหมายเหตุบนสุดของไฟล์
- งานใน repo นี้ที่เปลี่ยนสิ่งที่ผู้ใช้เห็นหรือทำ — endpoint / สิทธิ์ / ข้อความ error ที่ขึ้นหน้าจอ / พฤติกรรมของ widget และแชท / ค่าใน `.env` ที่ผู้ดูแลต้องตั้ง — ให้เปิดคู่มืออ่านหัวข้อที่เกี่ยวข้อง
- ไม่ตรงกับของจริง → แก้คู่มือให้ตรงในงานเดียวกัน (รวมหัวข้อ "แก้ปัญหาที่พบบ่อย" และป้ายสิทธิ์ของหัวข้อ)
- ฟีเจอร์ใหม่ที่ผู้ใช้ต้องรู้ → เพิ่มหัวข้อหรือข้อย่อยในคู่มือ
- ตอนส่งงาน / เปิด PR ให้บอกว่าแก้คู่มือตรงไหน หรือบอกว่าเช็กแล้วไม่ต้องแก้

## 2. UI ที่มีอยู่แล้ว ให้ใช้ของเดิม ห้ามสร้างแบบใหม่

- ใช้กับ UI ของ widget (`widget/`) และหน้าคอนโซลที่เพิ่มคู่กับงานฝั่งนี้ — หาของที่ใช้อยู่ก่อนแล้วใช้ตัวเดิม หน้าตาจะได้เหมือนกัน
- ของกลางฝั่งคอนโซลดูตารางใน `CLAUDE.md` ของ repo `ai-office-report`
- ของเดิมไม่พอ → ขยายตัวเดิมแทนการทำตัวใหม่ · จำเป็นต้องมีแบบใหม่จริง → อธิบายว่าทำไม และทำเป็นของกลางที่ใช้ต่อได้
