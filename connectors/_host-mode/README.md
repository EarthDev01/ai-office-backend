# connector แบบโหมด host (เก็บไว้อ้างอิง — ไม่ถูกโหลด)

ชุดนี้ชี้ไปที่เส้น `/api/ai/read/*` ที่เคยเพิ่มใน GOTOPOPOFFICE / office-api-v10
ทีมเลือกทางที่ 1 (โหมด browser — ไม่แก้หลังบ้าน) เมื่อ 24/09/2026 จึงย้ายมาไว้ที่นี่
โฟลเดอร์ขึ้นต้นด้วย `_` และไม่มี host.yaml ที่ระดับนี้ → `LoadDir` ข้าม
